# USB Kontrol-Blocker

# USB depolama cihazlarını devre dışı bırakma
# Registry Key: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\USBSTOR
# Value: Start
# Data: 4 (Disabled)

# PowerShell ile registry değişikliği
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\USBSTOR" -Name "Start" -Value 4

# USB'yi tekrar etkinleştirme
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\USBSTOR" -Name "Start" -Value 3

# Belirli USB cihazlarına izin verme (Device ID ile)
# Group Policy Management Console'da:
# Computer Configuration → Administrative Templates → System → Device Installation → Device Installation Restrictions

# Belirli donanım ID'lerine izin verme
# Allow installation of devices that match any of these device IDs: USB\VID_1234&PID_5678
